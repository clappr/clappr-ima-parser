describe('VASTManager', () => {
  describe('request method', () => {
    test.todo('returns a promise')
    test.todo('returns a rejected promise if adData is not received')
    test.todo('returns the ad content after the returned promise is resolved')
    test.todo('returns one error after the returned promise is rejected')
  })
})
